# 复制为 .env.local 后按需取消注释
# 仅在使用「两条 ngrok」且前端与后端域名不同时填写（无尾斜杠）
# VITE_API_BASE_URL=https://your-backend-tunnel.ngrok-free.app

# 可选：后端不在 localhost:8080 时，供 vite.config 代理使用（仅开发）
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
